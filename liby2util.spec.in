@HEADER-COMMENT@

# neededforbuild  gpp libgpp libxcrypt libxcrypt-devel yast2-devtools doxygen libpng tcl expect dejagnu openssl-devel pkgconfig zlib zlib-devel libxml2 libxml2-devel

@HEADER@
Summary:	-

%description
-

@HEADER-DEVEL@
Summary:	-
Requires: glibc-devel libstdc++-devel zlib-devel openssl-devel pkgconfig

%description devel
-

@PREP@

@BUILD@

@INSTALL@

@CLEAN@

%files
%defattr(-,root,root)
%{_libdir}/lib*.so.*

%files devel
%defattr(-,root,root)
%{_libdir}/lib*.so
%{_libdir}/lib*.la
@includedir@
@pkgconfigdir@/@RPMNAME@.pc
%doc @docdir@
